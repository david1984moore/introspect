# ============================================
# Email Service (Resend)
# ============================================
# Get from: https://resend.com/api-keys
# After signing up, go to API Keys → Create API Key
# Copy the key that starts with "re_" - you won't see it again!
RESEND_API_KEY=re_SNxNUDRk_6WnK8WXCF8SSMV3wvntSVzy7

# Email sender address
# For DEVELOPMENT/TESTING: Use onboarding@resend.dev (works immediately, no verification needed)
# For PRODUCTION: Use introspect@applicreations.com (requires domain verification)
INTROSPECT_FROM_EMAIL=Introspect <onboarding@resend.dev>

# ============================================
# Contact Information (Server-side)
# ============================================
# Your business email address (what clients will see)
DAVID_EMAIL=solutions@applicreations.com

# Your phone number (any readable format)
DAVID_PHONE=(302) 423-0215

# Calendly scheduling URL
# Get from: https://calendly.com (create free account if needed)
DAVID_CALENDLY_URL=https://calendly.com/app/personal/profile

# ============================================
# Contact Information (Client-side)
# ============================================
# These are exposed to the browser, so use the same values
# but with NEXT_PUBLIC_ prefix
NEXT_PUBLIC_DAVID_EMAIL=solutions@applicreations.com
NEXT_PUBLIC_DAVID_PHONE=(302) 423-0215
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/app/personal/profile

# ============================================
# Optional Configuration
# ============================================
# Base URL for PDF generation (defaults to http://localhost:3000)
# Only change if your API is hosted elsewhere
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

